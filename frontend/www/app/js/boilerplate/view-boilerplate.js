// Use this as a quick template for future views
define([
    'jquery',
    'underscore',
    'backbone'
], function ($, _, Backbone) {

    var MyView = Backbone.View.extend({
        initialize:function (options) {
        },
        render:function () {
        }
    });

    return MyView;
});
